import { CSSProperties } from 'react';

const TRIGGER_LIST = [];

interface Props {
  id: string;
  triggerType: string;
}
function TriggerEditBlock({}: Props) {
  return <div>trigger..</div>;
}

export default TriggerEditBlock;
